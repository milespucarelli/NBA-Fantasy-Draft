<h1><%= "#{@team.hometown} #{@team.name}" %></h1>
<ul>
<% @team.players.each do |player| %>
  <li><%= link_to player.name, player_path(player)%></li>
<% end %>
</ul>

<%# button_to "Edit Draft", edit_draft_path %>
<%# button_to "Edit Team Info", edit_team_path %>
